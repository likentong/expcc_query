dependencies {

}

task jarTests(type: Jar, dependsOn: testClasses) {
    from sourceSets.test.output
}

configurations {
    tests {
        extendsFrom testRuntime
    }
}

artifacts {
    tests jarTests
}